"use strict";

var OPC = {

    /**
     *  電源ON
     *
     *  @param {Function} cb
     *  @memberof OPC#
     */
    execPwon: function (cb) { // eslint-disable-line
        // BLEの仕様が公開されていないため保留
    }

};

module.exports = OPC;
